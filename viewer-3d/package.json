{
  "name": "aspergillum-model-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Viewer web isolado para inspeção dos modelos Bedrock do Aspergillum.",
  "scripts": {
    "sync-assets": "node scripts/sync-assets.mjs",
    "test": "node --test tests/*.node.mjs",
    "capture": "npm run sync-assets && node scripts/capture-models.mjs",
    "dev": "npm run sync-assets && vite",
    "build": "npm test && npm run sync-assets && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "playwright": "1.58.2",
    "vite": "8.2.0"
  }
}
